# ============================================
# Test Generator Web App - Environment Variables
# ============================================

# Claude Code Authentication (REQUIRED)
# --------------------------------------------
# Steps to obtain the token:
# 1. Install Claude Code CLI: npm install -g @anthropic-ai/claude-code
# 2. Authenticate: claude auth login
# 3. The token is stored at ~/.claude/config.json under "oauthToken"
#
# Alternatively, you can use the Anthropic API key directly.
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here

# Optional: Use Anthropic API instead of Claude CLI
# ANTHROPIC_API_KEY=sk-ant-...

# Server Configuration
# --------------------------------------------
PORT=3000
NODE_ENV=production

# Default Provider
# Options: elevenlabs, vapi, viernes
DEFAULT_PROVIDER=viernes

# ElevenLabs API Key (REQUIRED for elevenlabs provider)
# --------------------------------------------
ELEVENLABS_API_KEY=sk_your_elevenlabs_api_key_here

# SSH Bastion Configuration (REQUIRED for RDS access)
# --------------------------------------------
SSH_HOST=3.216.195.253
SSH_USER=ubuntu

# SSH Private Key (REQUIRED for SSH tunnel)
# Paste the entire content of your private key (ssh/id_rsa)
# For Render: Use environment variable, paste full key content
# For local: Run this command to get the formatted value:
#   cat ssh/id_rsa | base64
# Then set: SSH_PRIVATE_KEY="$(cat ssh/id_rsa)"
SSH_PRIVATE_KEY=your_ssh_private_key_content_here

# RDS Database Configuration (REQUIRED)
# --------------------------------------------
RDS_HOST=your-rds-endpoint.rds.amazonaws.com
RDS_PORT=3306

# MySQL Database Credentials (REQUIRED)
# --------------------------------------------
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=your_mysql_user
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=your_database_name
